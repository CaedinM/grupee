# Pinned exactly: Railway rebuilds from this file on every deploy, so an
# unpinned range would silently ship a different FastAPI/SQLAlchemy than the one
# the smoke test passed against. Bump deliberately, then re-run smoke_test.sh.
fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
alembic==1.18.5
pydantic==2.13.4
python-multipart==0.0.32
psycopg2-binary==2.9.12
redis==8.0.1
python-dotenv==1.2.2
PyJWT[crypto]==2.13.0
boto3==1.43.53
