# Loaded by `vite --mode production` (npm run dev:prod) and the default
# `vite build`, merged on top of .env. Public backend URL only — no secrets —
# so this file is committed; the Clerk key stays in the gitignored .env.
VITE_API_URL=https://wyat-production.up.railway.app
