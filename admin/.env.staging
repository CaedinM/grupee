# Loaded by `vite --mode staging` (npm run dev:staging / build:staging), merged
# on top of .env. Public backend URL only — no secrets — so this file is
# committed; the Clerk key stays in the gitignored .env.
VITE_API_URL=https://wyat-staging.up.railway.app
